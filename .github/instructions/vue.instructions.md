---
applyTo:
  - src/**/*.vue
---

Composition API + `<script setup>`. Typed props/emits; `ref`/`computed`; scoped styles + `base.css` tokens. PascalCase files, kebab-case tags. Prefer GlassPane, CodeBlock.
